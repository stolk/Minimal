#include "vmath.h"
#include "rendercontext.h"

extern int  odb_spawn_plane( const char*name, vec3_t pos );
extern int  odb_spawn_trafficcone( vec3_t pos );

extern void odb_draw_groundplane( const rendercontext_t& rc, int groundplaneid);
